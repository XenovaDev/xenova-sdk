# @xenova/engine

Off-chain runtime for Xenova: runs automation jobs, listens for events, executes actions, and sends webhooks.

This package is a minimal stub for the engine — production-ready implementation would include a job store, worker pool, and queue system.
